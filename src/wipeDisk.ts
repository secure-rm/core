/* import { standards } from './standards'

export function wipeDisk () {
  standards['GOST_R50739-95'].wipe({ device: '\\\\.\\PhysicalDrive1', deviceSize: 2004877312, chunkSize: 1024 * 1024 * 64 })
    .then(() => console.log('done'))
    .catch(console.error)
} */
